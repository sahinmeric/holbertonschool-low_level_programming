#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * mul - returns the largest of 3 numbers
 * @a: first value for the multiply
 * @b: second value for the multiply
 * Return: result of multiplacation
 */

int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
