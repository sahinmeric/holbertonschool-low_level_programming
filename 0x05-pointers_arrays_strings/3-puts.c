#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: The string that to be printed to stdout.
 *
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
