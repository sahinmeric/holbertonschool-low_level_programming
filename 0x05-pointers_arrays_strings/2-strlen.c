#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - swaps the values of two integers.
 * @s: This is the string whose length is to be found.
 *
 * Return: 0 success
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
